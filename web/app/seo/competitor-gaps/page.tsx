import{CompetitorGapWorkspace}from"@/components/competitor-gap-workspace";import{getCompetitorReport,getCompetitorTargets}from"@/lib/api";export default async function Page(){const targets=await getCompetitorTargets().catch(()=>[]);const report=targets[0]?await getCompetitorReport(targets[0]).catch(()=>null):null;return <div className="page module-page"><div className="page-heading"><div><span className="eyebrow">SEO intelligence</span><h1>Competitor Gaps</h1><p>Observed tracked-SERP, GSC, crawl, and URL-matched GA4 evidence only.</p></div></div><CompetitorGapWorkspace report={report}/></div>}
